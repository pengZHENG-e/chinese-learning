# Vercel Marketplace → Storage → Upstash Redis (these are auto-injected by Vercel)
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Resend (https://resend.com/api-keys)
RESEND_API_KEY=

# Optional: custom From address once you've verified a domain in Resend.
# Defaults to "学中文 <onboarding@resend.dev>" which only sends to your Resend account email.
# EMAIL_FROM=
